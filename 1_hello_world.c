/**
 * Preprocessor, replaces text stating with `#` with actual text before compilation begins
 * A standard lib that has i/o functions replaces include??
 * The output code of preprocessing is called expanded code
 * expanded code will be processed by compiler to machine code
 *
 */
#include <stdio.h>

/** main function that returns an integer
 * Return: 0
 */

int main(void)
{
	printf("Hello World!\n");
	return (0);
}
